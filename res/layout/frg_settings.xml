<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:orientation="vertical"
              android:padding="@dimen/ten_device_pixels"
              android:layout_width="match_parent"
              android:layout_height="match_parent">

    <ImageView android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:src="@drawable/interval_100"
            android:layout_centerInParent="true"/>

    <TextView android:layout_above="@+id/contactButton"
              android:layout_centerHorizontal="true"
              android:layout_marginBottom="@dimen/eight"
              android:textSize="@dimen/small_text"
              android:id="@+id/icons8text"
              android:layout_width="wrap_content"
              android:clickable="true"
              android:layout_height="wrap_content"/>



    <Button android:id="@+id/contactButton"
            android:paddingLeft="@dimen/eight"
            android:drawableStart="@drawable/contact_50"
            android:drawableLeft="@drawable/contact_50"
            android:layout_alignParentBottom="false"
            android:layout_marginBottom="@dimen/ten_device_pixels"
            android:layout_above="@+id/shareButton"
            style="@style/buttonStyle"
            android:background="@drawable/plus_minus_selector"
            android:text="@string/contact"/>

    <Button android:id="@+id/shareButton"
            android:paddingLeft="@dimen/eight"
            android:drawableStart="@drawable/share_50"
            android:drawableLeft="@drawable/share_50"
            style="@style/buttonStyle"
            android:background="@drawable/share_button_selector"
            android:text="@string/share"/>

</RelativeLayout>